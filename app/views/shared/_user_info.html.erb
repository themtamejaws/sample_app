<%= link_to gravatar_for(current_user), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "view my profile", current_user %>
</span>
<span>
  <%= pluralize(current_user.microposts.count, "micropost") %>
  <br>
  <%= pluralize(current_user.orders.count, "order") %> pending
  <br>
  <%= pluralize(current_user.protocols.count, "protocol") %>
</span>
